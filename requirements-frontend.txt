# ============================================================
# 角色 C: 前端开发
# 安装: pip install -r requirements-frontend.txt
# 大小: ~15 MB（只有 Flask + 依赖，不需要 torch）
#
# 开发方式 1（推荐）: 只写 HTML/CSS/JS，不需要后端
#   用浏览器直接打开 frontend/templates/index.html
#   或者用 VS Code Live Server 插件预览
#
# 开发方式 2: 启动 Flask 看完整效果
#   pip install -r requirements-frontend.txt
#   python run.py
#   页面可以正常渲染，但调用 API 会返回 503 错误（模型未就绪）
# ============================================================
flask==3.1.3
flask-cors==6.0.5
jinja2==3.1.6
waitress==3.0.2
