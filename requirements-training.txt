# ============================================================
# 角色 B: 数据与模型训练
# 安装: pip install -r requirements-training.txt --extra-index-url https://download.pytorch.org/whl/cpu
# 大小: ~3.5 GB（torch + transformers + datasets）
# 建议: 在 D 盘创建虚拟环境，模型文件会很大
# ============================================================
accelerate==1.14.0
datasets==5.0.0
numpy==2.5.0
pandas==3.0.3
peft==0.19.1
sacrebleu==2.6.0
torch==2.12.1+cpu
torchvision==0.27.1+cpu
transformers==5.12.1
tqdm==4.68.3
