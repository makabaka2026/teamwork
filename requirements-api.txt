# ============================================================
# 角色 D: API 路由 + 测试
# 安装: pip install -r requirements-api.txt --extra-index-url https://download.pytorch.org/whl/cpu
# 大小: ~3 GB（需要完整后端才能跑集成测试）
#
# 如果只做单元测试（不加载模型），可以只用 requirements-frontend.txt + pytest
# ============================================================
flask==3.1.3
flask-cors==6.0.5
jieba==0.42.1
numpy==2.5.0
pillow==12.3.0
pypinyin==0.55.0
pytest==8.4.0
requests==2.34.2
snownlp==0.12.3
torch==2.12.1+cpu
transformers==5.12.1
waitress==3.0.2
